--- a/scripts/modules/task-manager/update-single-task-status.js
+++ b/scripts/modules/task-manager/update-single-task-status.js
@@ -27,13 +27,16 @@ async function updateSingleTaskStatus(
 	// Check if it's a subtask (e.g., "1.2")
 	if (taskIdInput.includes('.')) {
 		const [parentId, subtaskId] = taskIdInput
-			.split('.')
-			.map((id) => parseInt(id, 10));
+			.split('.');
+		
+		const parentIdNum = parseInt(parentId, 10);
+		const subtaskIdNum = parseInt(subtaskId, 10);
 
 		// Find the parent task
-		const parentTask = data.tasks.find((t) => t.id === parentId);
+		const parentTask = data.tasks.find((t) => 
+			t.id === parentIdNum || t.id === parentId || t.id == parentId);
 		if (!parentTask) {
-			throw new Error(`Parent task ${parentId} not found`);
+			throw new Error(`Parent task ${parentId} not found`);
 		}
 
 		// Find the subtask
@@ -41,9 +44,10 @@ async function updateSingleTaskStatus(
 			throw new Error(`Parent task ${parentId} has no subtasks`);
 		}
 
-		const subtask = parentTask.subtasks.find((st) => st.id === subtaskId);
+		const subtask = parentTask.subtasks.find((st) => 
+			st.id === subtaskIdNum || st.id === subtaskId || st.id == subtaskId);
 		if (!subtask) {
 			throw new Error(
 				`Subtask ${subtaskId} not found in parent task ${parentId}`
 			);
 		}